# En desarrollo (npm run dev) Vite ya redirige /api al backend por su
# propio proxy (ver vite.config.ts), así que esta variable no es necesaria.
# Solo se usa en el build de producción cuando el frontend se sirve desde un
# origen distinto al del backend.
VITE_API_BASE_URL=/api
