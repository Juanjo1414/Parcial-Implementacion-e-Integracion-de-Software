spring:
  datasource:
    url: jdbc:h2:mem:camelracing_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop   # crea las tablas al iniciar el test, las borra al terminar
    database-platform: org.hibernate.dialect.H2Dialect

jwt:
  secret: "clave-secreta-de-prueba-de-al-menos-32-caracteres-para-tests"
  expiration-ms: 3600000